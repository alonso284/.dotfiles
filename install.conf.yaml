- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.zshenv: zshenv
    ~/.gitconfig: gitconfig
    ~/.vscode: vscode/
    ~/.cf: cf/
    ~/template: template/
    ~/.config: config/
